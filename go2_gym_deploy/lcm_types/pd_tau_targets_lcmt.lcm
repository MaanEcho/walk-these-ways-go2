struct pd_tau_targets_lcmt
{
    double q_des[12];   // 目标关节位置
    double qd_des[12];  // 目标关节速度
    double tau_ff[12];  // 前馈扭矩
    double kp[12];  // 关节电机位置增益kp
    double kd[12];  // 关节电机速度增益kd
    int64_t timestamp_us;   // 时间戳，单位：微秒
    int64_t id; // ID（不知道是什么的ID）
    int64_t robot_id;     // 机器人ID
    double se_contactState[4];  // 足端接触状态
}